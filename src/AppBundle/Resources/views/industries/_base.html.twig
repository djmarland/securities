{% block body %}
    <div class="grid">
    <div class="g 1/4@xl">
        {% include finder.templatePath with finder.vars only %}
    </div>
    <div class="g 3/4@xl">
        <div>
            <h1 class="h-b">{{ industry.name }}</h1>
            {% include entityNav.templatePath with entityNav.vars only %}
            <div class="g">
                {% include('@partials/advert.html.twig') with { adType : 'overview'} %}
            </div>
        </div>
        {% block content %}{% endblock %}
    </div>
    </div>
{% endblock %}