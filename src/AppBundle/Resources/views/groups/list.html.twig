{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <h1 class="g-unit">{{ meta.title }}</h1>
    <ul class="list--unstyled">
    {%  for group in groups %}
        <li class="g-unit">{% include group.templatePath with group.vars only %}</li>
    {%  endfor %}
    </ul>
{% endblock %}
