<IfModule mod_rewrite.c>
    Options +FollowSymlinks
    RewriteEngine On

    # Explicitly disable rewriting for front controllers
    RewriteRule ^/isin/public/app_dev.php - [L]
    RewriteRule ^/isin/public/app.php - [L]

    # Fix the bundles folder
    RewriteRule ^bundles/(.*)$ /isin/public/bundles/$1  [QSA,L]

    RewriteCond %{REQUEST_FILENAME} !-f
    # Change below before deploying to production
    RewriteRule ^(.*)$ /isin/public/app.php [QSA,L]
    #RewriteRule ^(.*)$ /isin/public/app_dev.php [QSA,L]
</IfModule>