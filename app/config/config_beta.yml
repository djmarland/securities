imports:
    - { resource: config.yml }

parameters:
    app.config:
        hostname: "https://www.isinanalytics.com"
        title: "ISIN Analytics [beta]"
        allowAdverts: false

security:
    firewalls:
        default:
            anonymous: false
            http_basic: ~